class BookController {
    constructor(bookView, bookService) {
        this.bookService = bookService;
        this.bookView = bookView;
    }

    //Get all books
    //Get a books by id
    //Get a book by idClient
    //Get a book by dniClient
}